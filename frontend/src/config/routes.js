const routes = {
    home: '/',
    category: '/category',
    users: '/users',
    products: '/products',
    product: '/product/:slug',
    categories: '/categories',
    auth: '/auth',
};

export default routes;
